1) 'docker-compose up --build'

2) data-data kesimpan di folder data

3) port mongodb --> localhost:8081

4) jalanin 'store_data.py'

5) cek port 8081 lagi, seharusnya data weather api udh muncul
-- data waqi masih otw